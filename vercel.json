{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "services": {
    "web": {
      "root": ".",
      "framework": "nextjs",
      "buildCommand": "sed -i -e 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/schema.prisma && npx prisma generate && npx prisma db push --skip-generate && npx tsx scripts/prod-init.ts && npm run build",
      "functions": {
        "app/api/events/stream/route.ts": { "maxDuration": 60 },
        "app/api/chat/route.ts": { "maxDuration": 60 }
      }
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": { "service": "web" }
    }
  ]
}
